cantera
numpy
numba
scipy
shapely
pillow
openpyxl
flask
gunicorn
pytest
awscli
boto3
# Optional GPU backend. Install only on a host with CUDA + NVIDIA GPU and
# pick the wheel matching your CUDA version. Not required for the default
# (NumPy/Numba) path. Activate with CFD_BACKEND=cupy.
# cupy-cuda12x
# cupy-cuda11x
